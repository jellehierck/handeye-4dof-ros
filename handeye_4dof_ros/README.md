# ROS bindings for `handeye_4dof`

This is the ROS package for the pure-Python `handeye_4dof` package.

## Install instructions

Clone this repository into your ROS 2 workspace.

Build with:

```bash
colcon build --symlink-install --packages-up-to handeye_4dof_ros
```
